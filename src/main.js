import MainApp from './MainApp';
import Dom from 'react-dom/lib/ReactDOM';
import React from 'react/lib/React';

// Initialize the app
Dom.render(
    <MainApp />,
    document.getElementById('react-mount')
);
